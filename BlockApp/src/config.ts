// Change this to your PC's local IP address (visible from Android on the same Wi-Fi)
// Run `ip addr show eth0` in WSL2 to find your IP, or check Windows ipconfig
export const API_BASE_URL = 'http://192.168.1.43:8001/api';